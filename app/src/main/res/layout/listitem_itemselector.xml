<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:padding="@dimen/divider_padding"
    android:clickable="true"
    android:background="?android:attr/selectableItemBackground"
    android:id="@+id/lyt_listitem"
    android:orientation="horizontal">
    <com.android.chooseasy.CircleImageView
        android:layout_width="@dimen/image_bullet_size"
        android:layout_height="@dimen/image_bullet_size"
        android:id="@+id/img_profile_mini"
        android:src="@color/colorPrimaryLight"
        android:layout_marginRight="@dimen/gid_column_space"/>
    <TextView android:id="@+id/select_title"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="center_vertical"
        android:text="Selectable Title"
        android:textColor="@color/black"
        app:layout_gravity="center_vertical"/>
</LinearLayout>